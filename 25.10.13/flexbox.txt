Flexbox Container: 전체 정렬 규칙을 설정
- display: flex; -> Flexbox 사용 선언 
- justify-Content: 가로 정렬 방식 결정 (space-between(양 끝 분리), center(가운데 정렬))
- flex-direction: 가로 방향 결정 (row, column)
- align-items: 세로 정렬 방식 결정 (center(수직 가운데 정렬))

Flexbox Item: 정렬 규칙에 따라 배치
- flex: 공간 비율
- order: 배치될 순서 결정